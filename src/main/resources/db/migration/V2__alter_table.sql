-- Add Unique constraint to users' email
CREATE TABLE order_item
(
    sequence   INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    order_id   VARCHAR(255)                             NOT NULL,
    quantity   INTEGER                                  NOT NULL,
    price      DOUBLE PRECISION,
    product_id INTEGER,
    CONSTRAINT pk_orderitem PRIMARY KEY (sequence, order_id)
);

CREATE TABLE orders
(
    order_id   VARCHAR(255)                NOT NULL,
    user_id    INTEGER                     NOT NULL,
    order_date TIMESTAMP WITHOUT TIME ZONE NOT NULL,
    CONSTRAINT pk_orders PRIMARY KEY (order_id)
);

CREATE TABLE products
(
    product_id  INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    description VARCHAR(100)                             NOT NULL,
    CONSTRAINT pk_products PRIMARY KEY (product_id)
);

-- Alter role type and set it as not nullable
ALTER TABLE users
ADD CONSTRAINT uc_users_email UNIQUE (email);
ALTER COLUMN role TYPE varchar(8),
ALTER COLUMN role SET NOT NULL;

ALTER TABLE users
ADD CONSTRAINT chk_role_valid CHECK (role IN ('admin', 'customer'));

ALTER TABLE order_item
    ADD CONSTRAINT uc_orderitem_product UNIQUE (product_id);

ALTER TABLE users
    ADD CONSTRAINT uc_users_email UNIQUE (email);

ALTER TABLE users
    ADD COLUMN password NOT NULL;

ALTER TABLE order_item
    ADD CONSTRAINT FK_ORDERITEM_ON_ORDER FOREIGN KEY (order_id) REFERENCES orders (order_id);

ALTER TABLE order_item
    ADD CONSTRAINT FK_ORDERITEM_ON_PRODUCT FOREIGN KEY (product_id) REFERENCES products (product_id);

ALTER TABLE orders
    ADD CONSTRAINT FK_ORDERS_ON_USER FOREIGN KEY (user_id) REFERENCES users (user_id);